<!DOCTYPE html
     PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
     "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="Content-Type" content="text/html;charset=utf-8" />
  <title>$_("Gnome Languages")</title>
  <link rel="stylesheet" href="${webroot}/data/main.css"/>
#if $rtl
  <link rel="stylesheet" href="${webroot}/data/rtl.css"/>
#end if
</head>
#if $rtl
<body dir="rtl">
#else
<body>
#end if

<div class="mainpage">

<h1>$_("Gnome Languages")</h1>

#if len($languages)
<p>$_("Gnome is being translated to following languages (%s):" % len($languages))</p>

<div class="col1">
<ul>
#set count = 1
#set col = 1
#for lang in $languages
<li style="font-size: 120%;">
  #if $lang.name
  <a href="${webroot}/languages/${lang.code}">${lang.name}</a>
  #else
  <a href="${webroot}/languages/${lang.code}">${lang.code}</a>
  #end if
</li>
  #set count = $count+1
  #if $count % (len($languages)/3+1) == 0
    #set col = $col+1
    </ul></div>
    <div class="col${col}"><ul>
  #end if
#end for
</ul>
</div>
#else
<p>$_("Gnome is currently not being translated to any language. :(")</p>
#end if


</div>
</body>
</html>
